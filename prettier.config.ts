// prettier.config.ts  (repo root)
import type { Config } from 'prettier'

const config: Config = {
  // create-vue ships no overrides — Prettier defaults are intentional.
  // Add project-wide rules here as needed, e.g.:
  // semi: false,
  // singleQuote: true,
}

export default config