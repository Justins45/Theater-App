import { describe, it, expect } from "vitest";

describe("admin", () => {
  it("placeholder — replace with real component tests", () => {
    const codeThatThrows = () => {
      throw new Error("something went wrong");
    };

    expect(() => codeThatThrows()).toThrow();
  });
});
